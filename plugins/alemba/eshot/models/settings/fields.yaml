# ===================================
#  Form Field Definitions
# ===================================

fields:

    api_key:
        label: API key
        type: text
        span: full
        required: true

    subaccount_id:
        label: Subaccount ID
        type: number
        default: 2
        span: left

    source_id:
        label: Source ID
        type: number
        default: 5
        comment: This defaults to the source ID for "API"
        span: right
